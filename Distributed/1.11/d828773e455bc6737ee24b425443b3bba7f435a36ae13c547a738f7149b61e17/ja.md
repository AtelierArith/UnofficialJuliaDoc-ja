```julia
isready(rr::RemoteChannel, args...)
```

[`RemoteChannel`](@ref) に値が格納されているかどうかを判断します。この関数はレースコンディションを引き起こす可能性があることに注意してください。結果を受け取る時点で、もはや真でない可能性があります。ただし、[`Future`](@ref) に対しては安全に使用できます。なぜなら、`Future` は一度だけ割り当てられるからです。
