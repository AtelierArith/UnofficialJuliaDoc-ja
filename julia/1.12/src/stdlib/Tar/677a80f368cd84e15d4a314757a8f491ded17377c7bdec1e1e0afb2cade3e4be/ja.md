```@meta
EditURL = "https://github.com/JuliaIO/Tar.jl/blob/master/docs/src/index.md"
```

`Tar` モジュールは、アーカイブの作成、アーカイブからの選択したファイルの抽出、およびメタデータへのアクセスを含む、tar アーカイブを扱うためのシンプルなインターフェースを提供します。

# Tar

```@docs
Tar.create
Tar.extract
Tar.list
Tar.rewrite
Tar.tree_hash
Tar.Header
```
